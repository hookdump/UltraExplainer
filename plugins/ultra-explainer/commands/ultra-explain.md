---
description: Explain a system, file, change, or concept as a self-contained HTML page
argument-hint: [what to explain]
---

Use the **ultra-explainer** skill to produce a self-contained HTML visual explanation of: $ARGUMENTS

Follow the synthesis method (`references/synthesis-method.md`): write the one-sentence charter, find the thesis with tension, harvest evidence from the *real* source with anchors, reconcile, then pick the lowest-ink representation that makes the thesis obvious in the first viewport. Choose the closest starting template in `templates/`, choose a design language for the subject (see `references/aesthetic-languages.md`), keep glow budgeted and toggleable, and verify both themes. Write the file, open it, and give a one-line chat summary with the path.
