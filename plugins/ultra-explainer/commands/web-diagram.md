---
description: Generate a standalone diagram (node-edge graph or themed Mermaid) as a self-contained HTML page
argument-hint: [what to diagram]
---

Use the **ultra-explainer** skill to draw: $ARGUMENTS

Pick the form on the cognitive job: a **node-edge graph** with routed arrows (`templates/luminous-flow.html`) when you control the layout and want hover-to-trace; **themed Mermaid** (`templates/mermaid.html`, see `references/mermaid.md`) for flowcharts, sequence, state, ER, or class diagrams. Keep it ≤12 nodes (raise altitude or split otherwise), emphasize the thesis path and dim the rest, and draw edges only where the relationship truly exists. Write the file, open it, summarize in one line.
