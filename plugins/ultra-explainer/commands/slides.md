---
description: Generate a self-contained slide deck (full-viewport, keyboard/scroll nav)
argument-hint: [topic / source to present]
---

Use the **ultra-explainer** skill in slide-deck mode to present: $ARGUMENTS

Read `references/slides.md` and start from `templates/slides.html`. First inventory the source and map every item to a slide — add slides rather than cramming. One idea per slide, big type, varied compositions (title, divider, split, diagram, KPI, quote, CTA). Keep the salience discipline (one focal point per slide, glow only on the T0 element). End on a clear next-step. Write the file, open it, summarize in one line.
